"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;
var _config = await jitiImport("nuxt/config");
var _url = await jitiImport("url"); // apps/frontend/nuxt.config.ts
var _default = exports.default =
(0, _config.defineNuxtConfig)({
  ssr: true,

  alias: {
    '@barterborsa/shared-types': (0, _url.fileURLToPath)(new URL('../../packages/shared/shared-types/src/index.ts', "file:///Users/macbook/Desktop/bazarx/apps/frontend/nuxt.config.ts"))
  },

  devtools: { enabled: true },

  app: {
    head: {
      title: 'BazarX - Modern Ticaret Platformu',
      meta: [
      { charset: 'utf-8' },
      { name: 'viewport', content: 'width=device-width, initial-scale=1' }],

      link: [
      { rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' }]

    }
  },

  css: ['~/assets/css/main.css'],

  modules: [
  '@pinia/nuxt',
  '@nuxtjs/tailwindcss',
  '@nuxtjs/i18n',
  'nuxt-icon'],


  i18n: {
    locales: [
    { code: 'tr', iso: 'tr-TR', file: 'tr.json' }],

    lazy: true,
    langDir: 'locales',
    defaultLocale: 'tr',
    strategy: 'no_prefix'
  },

  runtimeConfig: {
    public: {
      apiBase: process.env.NUXT_PUBLIC_API_BASE || 'http://localhost:3001/api/v1'
    }
  },

  // BFF Proxy: /api/v1 -> backend
  routeRules: {
    '/api/v1/**': {
      proxy: process.env.NUXT_API_PROXY_URL || 'http://localhost:3001/api/v1/**'
    }
  },

  postcss: {
    plugins: {
      tailwindcss: {},
      autoprefixer: {}
    }
  },

  build: {
    transpile: ['@barterborsa/shared-types']
  },

  vite: {
    ssr: {
      noExternal: ['@barterborsa/shared-types']
    }
  },

  experimental: {
    appManifest: false
  },

  compatibilityDate: '2024-04-03'
}); /* v9-0f7bcab0c571ec18 */
